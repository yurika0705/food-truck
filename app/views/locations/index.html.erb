<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>初心者向けホームページテンプレート tp_beginner5</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="ここにサイト説明を入れます">
  <meta name="keywords" content="キーワード１,キーワード２,キーワード３,キーワード４,キーワード５">
  <link rel="stylesheet" href="assets/style.css">
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jQuery.BgSwitcher/0.4.3/jquery.bgswitcher.min.js" integrity="sha512-cwDh42ooq48+o2kkbsn72Kwd9/Ghc34d48DFlNfOCRFJ1dM6EeIFs9d7PIBa4SSv6enwQAYmxsLZGgKOV0yE1A==" crossorigin="anonymous"></script>
  <!--[if lt IE 9]>
<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
<![endif]-->
</head>
<body>
  <div class="bg-switcher">
    <h1 class="bg">BgSwitcher テスト</h1>
  </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="jquery.bgswitcher.js"></script>
    <script>
        jQuery(function($) {
    $('.bg-switcher').bgSwitcher({
        images: ['1.jpg','2.jpg','3.jpg'], // 切り替え画像
        Interval: 5000, //切り替えの間隔 1000=1秒
        start: true, //$.fn.bgswitcher(config)をコールした時に切り替えを開始する
        loop: true, //切り替えをループする
        shuffle: false, //背景画像の順番をシャッフルする
        effect: "fade", //エフェクトの種類 "fade" "blind" "clip" "slide" "drop" "hide"
        duration: 1000, //エフェクトの時間 1000=1秒
        easing: "swing", //エフェクトのイージング "swing" "linear"
    });
});
    </script>
  <div id="container">
      <%= image_tag "mainimg2.jpg", class:"mainimg" %>
    <div id="contents">
      <section id="new">
        <h2>出店場所一覧</h2>
          <%# 投稿がある場合は、eachメゾッドで一覧表示する %>
          <div class="location-lists">
            <div class="prefecture-group">福岡県</div>
            <div class="location-list">
            <% @location1.each do |location| %> 
            <div class="location-image-content">
              <%= image_tag location.image.variant(resize: '200x200'), class: 'location-image' if location.image.attached? %>
              <%= link_to(location.place, location_path(location.id)) %>
            </div>
            <% end %>
            </div>
            <div class="prefecture-group">佐賀県</div>
            <div class="location-list">
            <% @location2.each do |location| %> 
            <div class="location-image-content">
                <%= image_tag location.image.variant(resize: '200x200'), class: 'location-image' if location.image.attached? %>
                <%= link_to(location.place, location_path(location.id)) %>
            </div>
            <% end %>
            </div>
            <div class="prefecture-group">長崎県</div>
            <div class="location-list">
            <% @location3.each do |location| %> 
            <div class="location-image-content">
                <%= image_tag location.image.variant(resize: '200x200'), class: 'location-image' if location.image.attached? %>
                <%= link_to(location.place, location_path(location.id)) %>
            </div>
            <% end %>
            </div>
            <div class="prefecture-group">熊本県</div>
            <div class="location-list">
            <% @location4.each do |location| %> 
            <div class="location-image-content">
                <%= image_tag location.image.variant(resize: '200x200'), class: 'location-image' if location.image.attached? %>
                <%= link_to(location.place, location_path(location.id)) %>
            </div>
            <% end %>
            </div>
            <div class="prefecture-group">大分県</div>
            <div class="location-list">
            <% @location5.each do |location| %> 
            <div class="location-image-content">
                <%= image_tag location.image.variant(resize: '200x200'), class: 'location-image' if location.image.attached? %>
                <%= link_to(location.place,  location_path(location.id)) %>
            </div>
            <% end %>
            </div>
            <div class="prefecture-group">山口県</div>
            <div class="location-list">
            <% @location6.each do |location| %> 
            <div class="location-image-content">
                <%= image_tag location.image.variant(resize: '200x200'), class: 'location-image' if location.image.attached? %>
                <%= link_to(location.place, location_path(location.id)) %>
            </div>
            <% end %>
            </div>
          </div>
      </section>
    </div>
    <!--/contents-->
    <footer> <small>Copyright&copy; <a href="index.html">SAMPLE SITE</a> All Rights Reserved.</small> <span class="pr">《<a href="https://template-party.com/" target="_blank">Web Design:Template-Party</a>》</span> </footer>
  </div>
  <!--/container-->
</body>
</html>