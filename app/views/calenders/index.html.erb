
  <p id="notice"><%= notice %></p>

  <div class="toppage">
  <h1 class="top">カレンダー</h1>
  <br>

  <div class="row">
  <div class="col-md-4">
    <div class="schedule">出店状況一覧</div>
  <table class="table">
    <thead class="thead-light">
      <tr>
        <th>キッチンカー名</th>
        <th>日程</th>
      </tr>
    </thead>

  <%= form_with(model: @calenders, url: location_calenders_path(:location_id), local: true) do |form| %>

    <div class="store-name">
      <div class="form-tag">キッチンカー名</div>
      <%= form.text_field :store_name %>
    </div>

      <div class="time">
      <div class="form-tag">日程</div>
      <%= form.datetime_select :start_time %>
    </div>

    <div class="submit">
      <%= form.submit %>
    </div>
  <% end %>
  </div>

  <br>


  <br>
  <div class="col-md-8">
  <%= month_calendar events: @calenders do |date, calenders| %>
    <%= date.day %>

    <% calenders.each do |calender| %>
      <div>
        <%= link_to calender.store_name, location_calenders_path(:location_id) %>
      </div>
    <% end %>
  <% end %>
  </div>
  </div>
  </div>